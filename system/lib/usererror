#include <systemerror.h>
#include <stdc++.h>
#include <std.h>
using namespace std;

void toSystemError(int code, char *tips){
    const char *realTips = tips;
    const int realCode = code;
    void t = breakdown(realCode,realTips);
    return;
}

int userError(int code, char *tips){
    clear()
    for(int i=0;i<10;i++){
        printf('-');
    }
    string tips = "A Error: " + tips;
    cout << tips << endl;
    return 0;
}

